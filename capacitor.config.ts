import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: '1.14AppResponsivePersonalizada',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
